//! Product identifiers re-exported from the bundled Issues package.
//!
//! The application shell keeps this path for CLI/control compatibility while
//! identifier ownership lives in `products/issues`.

pub use issues::ids::*;
