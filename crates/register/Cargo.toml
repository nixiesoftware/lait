[package]
name = "register"
version = "0.1.0"
edition = "2021"
rust-version = "1.92"
description = "Single-active selection with a lapsing override, for lait Worlds"
license = "PolyForm-Noncommercial-1.0.0"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }

[lints]
workspace = true
