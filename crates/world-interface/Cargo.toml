[package]
name = "world-interface"
version = "0.7.1"
edition = "2021"
rust-version = "1.91"
description = "Composable client interfaces for LAIT World applications"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
replica = { path = "../replica" }
runtime = { path = "../runtime" }
serde_json = "1"
tracing = "0.1"

[dev-dependencies]
schemars = "1"

[lints]
workspace = true
