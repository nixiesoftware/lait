# Build artifacts and fuzzing state. The corpus is deliberately NOT committed:
# a coverage-guided corpus is machine-specific, grows without bound, and is
# regenerated by the next run. What IS worth committing is a crash artifact,
# and those go into a regression test rather than into this directory.
target/
corpus/
artifacts/
coverage/
