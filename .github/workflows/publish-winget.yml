# CD: submit lait to the winget community repo (microsoft/winget-pkgs) on each
# release. Uses winget-releaser, which builds the manifest from the release's
# Windows zip and opens the PR.
#
# Triggered on the **Release** workflow completing (`workflow_run`) — which fires
# even though cargo-dist creates the release with the default GITHUB_TOKEN.
# `workflow_dispatch` is kept for manual re-runs.
#
# Needs a `WINGET_TOKEN` secret: a classic PAT with `public_repo`, from an account
# that has forked microsoft/winget-pkgs (the org GitHub App can't provide a
# user-namespace fork). Without it the job soft-skips, so it never blocks a release.
#
# winget-releaser is a third-party action and it necessarily receives that PAT — it
# opens the winget-pkgs PR as the fork user, and there is no OIDC equivalent to trade
# the secret away for (unlike the crates.io publisher). Two things bound the blast
# radius instead: the action is pinned to a full commit SHA rather than the mutable
# `v2` tag its maintainer re-points, and the token is step-scoped rather than living
# in job-level `env` where every step would see it.
name: Publish to winget

# NOT triggered by a release yet — it cannot succeed, so it would fail every one.
#
# Two blockers, both outside this file. The submit action is PowerShell and this
# job runs on a Linux runner (`pwsh: command not found`), and winget-pkgs only
# accepts an update to a package that already exists — the action says so:
# "does not exist in the winget-pkgs repository. Please add atleast one version
# of the package before using this action." So the first version has to be
# submitted by hand, and this job needs a windows runner, before automation has
# anything to automate.
#
# Until then this stays dispatch-only.
on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to publish (e.g. v0.4.3)"
        required: true

permissions:
  contents: read

jobs:
  winget:
    name: submit to winget-pkgs
    runs-on: ubuntu-latest
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event.workflow_run.conclusion == 'success' &&
       startsWith(github.event.workflow_run.head_branch, 'v'))
    steps:
      # WINGET_TOKEN is scoped to this step (to test presence) and to the submit step
      # below (which actually needs it) — never to the job, where it would sit in the
      # environment of every action that runs.
      - name: Resolve tag + gate on token
        id: gate
        env:
          GH_TOKEN: ${{ github.token }}
          DISPATCH_TAG: ${{ github.event.inputs.tag }}
          RUN_TAG: ${{ github.event.workflow_run.head_branch }}
          REPO: ${{ github.repository }}
          WINGET_TOKEN: ${{ secrets.WINGET_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          tag="${DISPATCH_TAG:-${RUN_TAG:-}}"
          if [ -z "$tag" ] || [ "${tag#v}" = "$tag" ]; then
            tag="$(gh release list --repo "$REPO" --limit 1 --json tagName --jq '.[0].tagName')"
          fi
          [ -n "$tag" ] || { echo "::error::could not resolve a release tag"; exit 1; }
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          if [ -n "${WINGET_TOKEN:-}" ]; then echo "ok=true" >> "$GITHUB_OUTPUT"
          else echo "::notice::WINGET_TOKEN not set — skipping winget submission."; echo "ok=false" >> "$GITHUB_OUTPUT"; fi

      # vedantmgoyal9/winget-releaser@v2 (lightweight tag; the maintainer re-points it)
      - name: Submit manifest
        if: steps.gate.outputs.ok == 'true'
        uses: vedantmgoyal9/winget-releaser@4ffc7888bffd451b357355dc214d43bb9f23917e
        with:
          identifier: NixieTechLLC.Lait
          release-tag: ${{ steps.gate.outputs.tag }}
          installers-regex: 'x86_64-pc-windows-msvc\.zip$'
          # The fork + WINGET_TOKEN live under omarbinyounes, not the repo org, so
          # override the default fork-user (which is the repository owner).
          fork-user: omarbinyounes
          token: ${{ secrets.WINGET_TOKEN }}
