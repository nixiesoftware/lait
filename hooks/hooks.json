{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v lait >/dev/null 2>&1 || echo 'lait plugin: the `lait` binary is not on your PATH, so its MCP server cannot start. Install Astrolabe from the signed first-party feed; see docs/INSTALL.md.'"
          }
        ]
      }
    ]
  }
}
